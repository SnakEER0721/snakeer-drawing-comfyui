# 更新日志

格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，
版本号遵循[语义化版本](https://semver.org/lang/zh-CN/)。

## [0.1.4] - 2026-09-22

**这一版解决的是"打包器会把你自己那份配置删掉"**，外加一个被它暴露出来的静默失败。
`--clean` 本来的意思是"重建前先把上次打包生成的东西清掉"，但它当时清的是**整个发布
目录**，只放过 `.git` —— 于是你手工放在发布目录里的 `config.json`（你挑好的底模）、
别名表、出图溯源记录**全都跟着被铲掉**，而且没有任何提示。

更糟的是第二件事：`config.json` 被删掉之后，服务**把它当成"全新安装"**，底模悄悄退回
写死的默认值，界面上一个提示都没有 —— 你以为在用自己挑的那个底模，其实没有。
两件事叠在一起，症状是"重建一次发布目录，我的设置就没了，而且看不出来"。

### 修复

- **`--clean` 会铲掉用户自己的本地文件**：清理逻辑改成"只清打包器自己生成的东西"。
  判据是"这东西是不是本次要发布的文件"，不是"它在不在这个目录里" —— 这两者以前被
  混为一谈，而 `EXCLUDED`（不发布）也**不等于**"可以删"。现在保留 `config.json`、
  `lora_aliases.json`、`_provenance.json`、`custom_prompt_options.json`（连同它们的
  `.bak` / `.tmp` / `.bak-<时间>` 变体）和 `.git`，其余照常清掉，并在输出里逐条打印
  `保留（不清）：…`，让"没删什么"也看得见。

- **`config.json` 不见了会被当成首次运行，静默退回默认底模**：现在会区分
  `config.json` 不在、和 `config.json` 在但读不出来 —— 后者本来就会报警，
  前者原来一声不响。
  判据落在**服务自己留下的、用户不会去动的东西**上：`_provenance.json`（出图时自动
  写的）、`lora_aliases.json` / `custom_prompt_options.json`（你在界面里配的）、
  以及产出目录里已经有出图的日期文件夹。命中任意一条就说明"这台机器用过这个应用"，
  于是报出这句话：现在实际在用的是哪个底模（点名）、你原来挑的那个没生效、
  以及怎么修（双击「安装模型.bat」重建一份带说明的 `config.json`）。
  真的首次运行仍然**完全安静** —— 新用户不该一开 app 就看到红标签。

  ⚠️ 界面上那个红标签（`配置有问题`）和状态栏文案**本来就有**，缺的只是那句信号：
  以前只有"文件在但坏了"才产生它。

- **接口读的是 import 时的快照**：`from paths import CONFIG_ERROR` 抄下来的是导入
  那一刻的值，而"配置没了"这条判据要到**启动时**才算得出来（它要看产出目录），
  算出来写回的是 `paths` 里的那个变量。结果就是：服务端算出来了、也打了日志，
  但接口返回的永远是旧的 `None`，**界面上什么都不显示**。现在接口读实时值。

### 新增

- **打包后紧跟一道闸：`dev/audit_clean_keep.py`** —— 造一份"该留 / 该清"的名字清单
  （13 个必须留、20 个必须清，外加 4 个"别管太宽"的反例，例如 `config.jsonc` 不在
  保护范围内），直接从 `make_release.py` 的源码里 AST 解析出判据本身再比对，
  所以它守的是**打包器现在真正在用的那条判据**，不是抄来的一份。
  为这个 `--clean` 的坑建的；`dev/run_regression.py` 把它排在**紧跟打包**那一步。

- **`tests/test_config_missing_warns.py`**（26 项）—— 上面那条修复的回归。
  真首次运行必须安静、真出过图必须出声、三个物证文件各验一遍、产出目录那条
  单独验（它走的是另一条代码路径），并且有一条反例：产出目录里**只有**
  服务自己建的 `_depth` / `_recycle` 时不许出声（否则每个新用户都会看到红标签）。

- **`tests/test_config_gone_e2e.py`** —— 端到端那条：从临时目录起一个**真服务**，
  真去敲 `/api/capabilities`，断言那句话确实到了接口上、界面确实认得这个字段。
  为什么要单独一条：上面那条测的是 `paths.py` 的**逻辑**，逻辑全对而**接线断掉**
  它照样全绿（比如接口又改回读快照）。A/B 验过：把接口改回读快照 → 立刻 2 项红；
  把启动时那次收口调用删掉 → 也立刻 2 项红。

### 测试

- `test_broken_config.py` 的【3】原来断言的是"`server` 拿到的是 `paths` 在 import
  时的那个值"——那其实是在**钉一个 bug**。改成钉新行为，并且加了一条反向断言：
  只改那个快照名字**必须不起作用**，否则这条在任何一种读法下都会绿，等于白测。

## [0.1.3] - 2026-09-22

**这一版解决的是"图会跑到产出目录根上去"** —— 你指定了"深度图放 `_depth`"，
可深度图还是一个个堆在产出目录根上，根层不属于任何一天，翻图的时候根本看不到
它们、也就没人想得起删。原因不在 `_depth` 那个设置上，而在深度工作流的存图节点
少了 `temp/` 这一层中转，于是 ComfyUI 把它当"没有子目录"存到了根上，而清理逻辑
只扫 `temp/` 底下，够不着。顺带修了四处审计出来的问题。

### 修复

- **深度图堆到产出目录根**：深度工作流里存图节点的文件名前缀从裸 `depth` 改成
  `temp/depth`。裸前缀时 ComfyUI 把图存到产出目录根（子目录为空），而清理只删
  `temp/` 底下的副本 —— 于是每解析一张就在根上留一个文件。改成走 `temp/` 中转后，
  根目录不再新增，清理也能正常扫到。用新旧两种前缀各跑一遍对照验证：旧前缀确实
  弄脏根目录，新前缀根目录 0 个。

- **LoRA 的「不兼容」判错了**：以前拿文件里的命名字段去猜。实测 ComfyUI 对 kohya
  和 diffusers **两套命名都能加载**（真底模的映射表 6970 条，两套命名都在里面：
  kohya 3346 条、diffusers 3624 条）。原判据把 diffusers 命名的**好**
  LoRA 判成不兼容 —— 它会让程序拒绝把一个能用的 LoRA 接进工作流，自检也会误告警。
  本机 26 个 LoRA 实测：改后误报 0 个。

- **`--clean` 会连 git 仓库一起铲平**：打包器的 `--clean` 用它自己那个"连只读位
  一起强删"的函数清目录，而 `.git/objects` 恰好带只读属性 —— 于是重建发布目录会
  把仓库**连历史一起删掉**。现在 `.git` 在保留名单里。

- **一个没装 opencv 的环境会让一道安全闸静默消失**：`poisson_blend.py` 原来在模块
  顶层 `import cv2`，于是 `import server` 的整条链在没有 opencv 的机器上直接失败，
  而那个测试把导入失败当成"环境不具备"、打印 SKIP 就退出 —— **它守的是"能不能读走
  硬盘上任意文件"，却可以在没有任何信号的情况下整道消失**。改成在真正用 cv2 的函数
  内部延迟导入。

- **"先用后 import"一直没被查出来**：语法检查只看"这个名字出现过没有"，不看它第几行
  才 import，于是"第 10 行用、第 30 行才 import"这种同样是运行时 `NameError` 的写法
  全部漏掉。实测 3 个文件中招（其中一个脚本的搜索路径还指错了目录，`import paths`
  直接失败 —— **它从来没跑起来过**）。现在比对"首次使用行"和"import 行"。
  另外这个检查脚本自己长期只打印结论、**从不给退出码**，报 FAIL 时退出码仍是 0，
  单独跑或写进 CI 就是个静默通过 —— 已补上。

- **上传文件名的后缀会进到请求头里**：客户端给的文件名里可以带换行，而服务端直接
  取 `splitext` 的后缀去拼 multipart 请求头，等于能借文件名往头里多插一行。
  现在只放行「`.` 加 1~5 位小写字母数字」，其余一律退回 `.png`。

- **LoRA 分类读不出元数据时是静默的**：原来直接吞掉异常，用户只看到"分类是
  Concept"，分不清是解析炸了还是这个 LoRA 本来就没元数据。改成落一条日志。

### 新增

- **发布前多一道闸：包和源头必须逐字节一致。** 这一版就是被它逼出来的 —— 上次推上
  GitHub 的那版**漏掉了四个已修好的问题**，而当时十二道检查全绿。原因是那些检查问的
  都是"包自己有没有毛病"（绝对路径、换行符、文档数字、临时文件……），**没有一条在问
  "包里的文件是不是源头的文件"**，而打包时间比修复时间早了五小时。现在这道闸逐个文件
  比内容，并且反向查"包里有、源头没有"的残留旧文件。

- **测试失败时把完整输出留在磁盘上**：原来失败只留一行摘要，证据随进程一起没了。

### 测试

- 新增：`test_param_http.py`（参数校验走真实 HTTP，19 项）、`test_auto_free.py`、
  `test_translate_threads.py`、`test_translate_xss.py`、`test_alias_kind_guard.py`。
- **一个测试会往用户的回收站里留东西**：它验完"删除"只断言"产出目录里没了"，
  但"删除"其实是**移进回收站**、文件并没消失 —— 于是断言一直绿，而每跑一次用户的
  回收站就多一个文件。实测连跑两次净增 2。现在用回收站前后差集认出自己的产物并清掉，
  并且把"确实是移进回收站而不是直接删"也断言出来。
- `test_imports.py`、`test_static.py` 的判据按上面两条修复同步收紧。

## [0.1.2] - 2026-09-19

**这一版解决的是"找不到 ComfyUI 的目录"** —— 以前只会在 C 盘几个默认位置猜，
没猜中就退回软件自己的文件夹（那个文件夹不存在），于是用户看到路径不对，
却完全不知道该改哪里。现在它会去读 **ComfyUI 桌面版自己写下的配置**。

### 修复

- **自动探测范围太窄**：以前只会用 `%LOCALAPPDATA%` 拼几个默认位置去猜。
  ComfyUI 桌面版把模型放在别的盘、或用的是便携版时，一律探不到。
  现在按下面的顺序问，**问到第一个就停**：
  1. 环境变量 `COMFYUI_ROOT`（你自己明确指定，优先级最高）
  2. ComfyUI 桌面版自己那份设置文件 —— 你在 ComfyUI 界面里选的那些目录
     （模型目录、输入目录、输出目录），**这份最准**
  3. ComfyUI 真正读的那份共享模型路径配置
  4. 桌面版登记表里记的安装位置（装在别的盘时靠它）
  5. `%LOCALAPPDATA%` 下的默认位置（纯猜，放最后）

  ⚠️ 输出目录**不是靠目录名认出来的**，是靠它挂在配置文件的哪个键上认的 ——
  实测有人的输出目录叫 `comfyout`，按名字根本猜不出来。

- **`install_models.py` 会把模型搬进一个 ComfyUI 根本不读的目录**，还打印
  "装好了"：自动探测失败时它退回软件自己的 `models` 文件夹，脚本自己把那个
  文件夹建出来（于是它"存在"了），接着就把十几个 GB 的模型搬进去。
  用户下次出图照旧"找不到底模"，而且不知道该怀疑哪一步。
  现在判据是**这个目录是哪来的**（不是"目录在不在"）：探测失败后的兜底目录
  一律**停下并退 2**，说清"ComfyUI 不读这里"，并给出两条出路。

- `install_models.py` 里"（来自：…）"那行原来是**自己拼的一句猜测** ——
  config.json 里没填就一律印"自动探测 / 环境变量"。现在照
  `paths.DIR_SOURCE` 记的念，把"这是兜底"如实说出来。

### 新增

- **`check_env.py` 会把每条路径的来源打出来**，例如
  `输出目录 存在: D:\comfyout（配置文件 config.json 里的 comfy_output）`。
  探测失败时会说清"自动探测没找到"，并给出照着能做的办法。
  （判断来源这件事以前**任何地方都不出声** —— 这正是这个问题难查的原因。）

- `dev/make_release.py --clean` 现在会先摘掉 git 对象的只读属性再删。
  实测 `.git/objects/**` 在 Windows 上带只读位（95 个文件里 91 个），
  报的是 `WinError 5 拒绝访问`，看起来像"有程序占着"，于是老提示
  "先关掉占用它们的程序"把人指去了完全错的方向（那一刻一个 git 进程都没有）。

## [0.1.1] - 2026-09-19

**这一版解决的是 LoRA 面板"说不清自己是怎么判断的"** —— 分类猜错了不报错、
不显示，用户只能自己猜；文件名是一串哈希时连猜都猜不了。

### 新增

- **内置 LoRA 资料库**（`lora_catalog.json`）：按文件 SHA256 从 Civitai 反查
  回公开信息（作者给的模型真名 / 版本 / 底模 / 作者声明的触发词 / 标签 /
  推荐权重 / 链接），**只存事实，不含任何本机路径**。装了同一个 LoRA 就自动
  显示真名、底模和触发词，分类也更准。
  - 查不到**不影响使用**：自动退回「文件元数据推的」→「文件名猜的」两层，
    界面上照实写「来源：文件名猜的」，不会编一个来源出来。
  - 想给自己的 LoRA 生成资料：`python dev/make_lora_catalog.py`
    （`--enrich` 只重抓 C站信息，`--no-net` 不联网）。
  - **不需要它就删掉** —— 只是分类变回"猜"，其他功能一律不受影响。
- **每条 LoRA 都写明"它是靠什么判断出来的"**：`你标的` / `C站数据` /
  `文件元数据推的` / `文件名猜的` / `按体积猜的` / `没判断出来`。
  前三种按实情显示，后三种会标黄提醒"建议自己改一下"。
- **界面上直接改名字和分类**：每条 LoRA 下面的「改名字/分类」按钮，
  弹窗里填中文名 + 选类型（角色 / 画风 / 画质增强 / 其他），保存即生效。
  **用户的判断永远压过软件的猜测**，写进 `lora_aliases.json`。
  （原来只能自己用记事本改 JSON —— 界面上只看得到、改不了。）
- LoRA 下拉框里中文名后面会注明它**是哪来的**：`（748cm画风 · C站自动）`、
  `（我的角色 · 你改的）`。不然自动填的中文名看着像自己以前起的，
  分不清"我改的名字到底被记住没有"。
- LoRA 行里会显示查到的 **C站真名**、**底模**、**作者推荐权重**、**`#标签`**。

### 修复

- LoRA 明明有别名却显示不出"这个分类是谁判的"（`kind_source` 只在最后一条
  分支上写，前几层判断走了却不留痕）。
- 有本地别名时，C站真名被整条藏起来 —— 于是 `8be1e5d2….safetensors` 这种
  纯哈希文件名，界面上一个字都看不出它到底是个什么东西。

## [0.1.0] - 2026-09-18

首个公开版本。

### 功能

- **中文提示词**：输入中文（`银发少女 站在窗边`），自动翻译成 Danbooru 标签。
  收录 32 万个标签，其中 5 万个有中文对照，带反向索引；
  手打英文原样透传，不会被模糊匹配成别的标签。
- **拼装面板**：按章节组织的中文选项（发色 / 表情 / 服装 / 姿势 / 场景 …），
  点选（可多选）后点「填入提示词框」一起加进去，也能自己加选项。
- **局部重绘**：画蒙版只重画蒙版内的部分，带羽化和外扩控制。
- **草图控制**：涂鸦（scribble）和骨架图（openpose）两种 ControlNet。
  手画的草图走涂鸦（默认）；openpose 要渲染好的骨架图才锁得住姿势。
- **构图迁移**：参考图先转深度图，再交给 union ControlNet。
- **参考图**：IPAdapter，把一张图的风格或人物带进新图。
- **放大**：RealESRGAN 动漫专用模型，等比放大（不会把 16:9 拉成 4:3）。
- **从成品图读回参数**：把出过的图丢回来，能还原当时的提示词和全部参数。
- **站内看图**：点缩略图在站内打开、`←` `→` 翻页、`Esc` 关闭。
- **删除 = 移到回收站**：误点不会永久丢失（出图是几十分钟的算力）。
- **底模切换**：界面右上角「参数」卡里直接换底模，写回 `config.json`。

### 首次运行的便利

- `启动UI.bat` 双击即用，不写死 Python 路径、不写死端口。
- `安装模型.bat` + 「刚需模型全部放这」文件夹：把从网盘下载的模型扔进去，
  双击脚本，自动放到 ComfyUI 的对应目录并改好配置。
- `check_env.py` 逐项自检，缺什么、放哪里、怎么修都会写清楚。

### 移植性

- **所有路径收敛到 `paths.py`**。此前路径逻辑散落各处，
  换台电脑就全线跑不起来。现在四级回退：
  `config.json` > 环境变量 > 自动探测 > 应用目录下的兜底。
- **端口可配**：`config.json` 的 `port`，没有就从 8765 起往上找第一个空闲端口。
- **底模不写死**：从 ComfyUI 的节点信息里读实际可选的底模列表。

### 安全

- 只监听 `127.0.0.1`，不对外暴露。
- 校验 `Origin` 与 `Host`，挡住跨源请求和 DNS rebinding。
- 写回 `config.json` 走白名单键，且是原子写。

[0.1.0]: https://github.com/SnakEER0721/snakeer-drawing-comfyui/releases/tag/v0.1.0
[0.1.1]: https://github.com/SnakEER0721/snakeer-drawing-comfyui/releases/tag/v0.1.1
[0.1.2]: https://github.com/SnakEER0721/snakeer-drawing-comfyui/releases/tag/v0.1.2
