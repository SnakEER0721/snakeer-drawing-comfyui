# ---- 换行：两个 .bat 绝对不能被 git 动 ----
# 它们是 GBK + CRLF。实测：.bat 变成裸 LF 之后 cmd 解析会错乱，报出和代码
# 内容毫无关系的错。core.autocrlf=true 的机器上，没有这一行的话提交时
# CRLF 会被转成 LF 存进仓库，Download ZIP 的用户拿到的就是坏的那份。
*.bat -text

# ---- 二进制，不要当文本处理 ----
*.png         binary
*.jpg         binary
*.jpeg        binary
*.ico         binary
*.sqlite3     binary
*.pth         binary
*.safetensors binary
*.ckpt        binary
*.bin         binary
