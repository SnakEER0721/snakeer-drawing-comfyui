把从网盘下载的模型文件全部放进这个文件夹，然后回到上一层，
双击「安装模型.bat」。脚本会自动把它们放到 ComfyUI 的对应目录，并改好配置。

不要改文件名 —— 脚本靠文件名认出它是哪个模型。改过名就认不出来了
（认不出来的文件脚本不会碰，只会在屏幕上列出来）。

需要哪些文件、各自是干什么的，见 README 里的「模型清单」一节。

放好之后可以跑 python check_env.py 确认 ComfyUI 那边也认出来了。
